import * as React from 'react';
import AppIa from './app-ia';

interface IAppProps {}

const App: React.FunctionComponent<IAppProps> = props => {
	const {} = props;

	return <AppIa />;
};

export default App;
